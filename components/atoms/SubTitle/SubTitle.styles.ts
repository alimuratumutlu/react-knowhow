import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  title: {
    borderTop: '1px solid #dcdcdc',
    paddingTop: 20,
    color: '#626262',
    fontWeight: 700,
  },
}));
